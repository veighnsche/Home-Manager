{
  programs.plasma = {
    enable = true;

    workspace = {
      # colorScheme = "BreezeDark";
      # theme = "breeze-dark";
      # Or use a global theme:
      lookAndFeel = "org.kde.breezedark.desktop";
    };
  };
}
