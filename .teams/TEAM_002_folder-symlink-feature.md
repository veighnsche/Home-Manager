# TEAM_002 – Folder Symlink Feature

- **Goal**: Extend `gather-files.nix` to support symlinking entire folders instead of recursing into them.
- **Context**: Currently `gather-files.nix` always recurses into directories. User wants to tag certain folders as "don't recurse, symlink the folder itself".
- **Status**: Planning phase.
